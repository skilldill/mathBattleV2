---
import Layout from "../layouts/Layout.astro";
---

<Layout>
	<div class="container">
		<div class="glassBlock">
			<div class="light" style="top: -100px; left: -80px;"></div>
			<div class="light" style="bottom: -100px; right: -80px;"></div>
			<div class="glassBlockContent">
				<h1 class="title">Math battle</h1>
				<p class="description">
					Math battle — это увлекательная игра, в которой каждый игрок проверяет свои знания и навыки в захватывающем математическом поединке. Испытай себя, прокачай логику и стань лучшим в мире математики!
				</p>
				<div class="telegramButtonsContainer">
					<a href="https://t.me/MathlyBattleBot" class="telegramButton">Открыть в Telegram</a>
					<a href="https://app.math-battle.ru" class="telegramButton browserButton">Открыть в браузере</a>
				</div>
			</div>
		</div>
	</div>
</Layout>

<style>
	.container {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		height: 100%;
		box-sizing: border-box;
		padding: 20px;
	}

	.title {
		font-size: 4rem;
		font-weight: 700;
		color: rgba(255, 255, 255, 0.9);
		text-align: center;
		margin-top: 0px;
		margin-bottom: 20px;
	}

	.description {
		font-size: 1.5rem;
		font-weight: 400;
		color: rgba(255, 255, 255, 0.7);
		text-align: center;
		line-height: 1.5;
	}

	.glassBlock {
		width: 100%;
		min-height: 580px;
		max-width: 600px;
		position: relative;
		border-radius: 36px;
		background-color: #273141;
		overflow: hidden;
	}

	.glassBlockContent {
		position: absolute;
		top: 1px;
		left: 1px;
		bottom: 1px;
		right: 1px;
		border-radius: 36px;
		background-color: #07090c99;
		z-index: 2;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		padding: 40px 40px;
		box-sizing: border-box;
	}

	.light {
		position: absolute;
		width: 300px;
		height: 300px;
		border-radius: 100%;
		background-color: rgba(255, 255, 255, 0.3);
		filter: blur(100px);
		webkit-filter: blur(100px);
		z-index: 1;
	}

	.telegramButton {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 12px;
		width: 100%;
		flex: 1;
		padding: 16px 32px;
		font-size: 1.25rem;
		font-weight: 600;
		color: rgba(255, 255, 255, 0.9);
		text-decoration: none;
		background-color: #273141;
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 16px;
		backdrop-filter: blur(10px);
		transition: all 0.3s ease;
		box-sizing: border-box;
	}

	.telegramButton:hover {
		background-color: #2f3b4d;
		transform: translateY(-2px);
	}

	.telegramButtonsContainer {
		display: flex;
		flex-direction: column;
		gap: 18px;
		width: 100%;
	}

	.browserButton {
		background-color: #2f4d3981;
	}

	.browserButton:hover {
		background-color: #2f4d39;
		transform: translateY(-2px);
	}

	@media (max-width: 768px) {
		.container {
			padding: 10px;
		}

		.title {
			font-size: 2.5rem;
		}

		.description {
			font-size: 1.25rem;
		}
	}
</style>
