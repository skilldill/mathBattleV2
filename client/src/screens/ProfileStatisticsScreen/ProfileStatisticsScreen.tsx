import { ScreenLayout } from "../../components";

export const ProfileStatisticsScreen: React.FC = () => {
    return (
        <ScreenLayout>
            <h1>Статистика</h1>
        </ScreenLayout>
    );
};